# terraform-aws-s3

### Explanation of Variables
`bucket_name` - name of bucket that matches aws naming rules for more information see https://docs.aws.amazon.com/AmazonS3/latest/userguide/bucketnamingrules.html
`bucket_versioning` - should typically always be true as it is the standard best practice in most situations to enable versioning.
